/* src/components/Profile/Profile.module.css */
.profileContainer {
    display: grid;
    grid-template-columns: 240px 1fr;
    gap: 2rem;
    align-items: flex-start;
    margin-top: 2rem;
}
.profileHeader {
    grid-column: 1 / -1;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: var(--bg-content);
    padding: 1.5rem;
    border-radius: var(--border-radius-lg);
    border: 1px solid var(--border-color);
}
.headerInfo { display: flex; align-items: center; gap: 1.5rem; }
.avatar { width: 80px; height: 80px; border-radius: 50%; border: 4px solid var(--accent-primary-solid); flex-shrink: 0; }
.userName { font-size: 1.75rem; font-weight: 700; color: var(--text-primary); }
.userGrade { font-size: 1rem; color: var(--text-secondary); }
.editProfileBtn { background: var(--accent-primary-solid); color: white; border: none; padding: 0.75rem 1.5rem; border-radius: var(--border-radius-md); font-family: inherit; font-size: 1rem; cursor: pointer; transition: opacity var(--transition-speed); flex-shrink: 0; }
.editProfileBtn:hover { opacity: 0.9; }
.profileSidebar { background-color: var(--bg-content); padding: 1rem; border-radius: var(--border-radius-lg); border: 1px solid var(--border-color); overflow: hidden; }
.sidebarLink { 
    display: flex; 
    align-items: center; 
    gap: 0.75rem; 
    width: 100%; 
    padding: 0.85rem; 
    border-radius: var(--border-radius-md); 
    font-size: 1rem; 
    color: var(--text-secondary); 
    background: none; 
    border: none; 
    text-align: right; 
    cursor: pointer; 
    margin-bottom: 0.2rem;
    margin-left: 0.5rem;
    transition: all var(--transition-speed);
    font-family: inherit; /* Ensures Vazirmatn font is used */
}
.sidebarLink:hover { background-color: var(--border-color); color: var(--text-primary); }
.sidebarLink.active { background-color: var(--accent-primary-solid); color: white; font-weight: 500; }
.sidebarLink.active svg { color: white; }
.profileContent { background-color: var(--bg-content); padding: 2rem; border-radius: var(--border-radius-lg); border: 1px solid var(--border-color); }
.contentSection { margin-bottom: 2.5rem; }
.contentSection:last-child { margin-bottom: 0; }
.sectionTitle { font-size: 1.25rem; font-weight: 700; color: var(--text-primary); margin-bottom: 1.5rem; padding-bottom: 0.5rem; border-bottom: 2px solid var(--accent-primary-solid); display: inline-block; }
.infoGrid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.5rem; }
.infoRow { display: flex; align-items: baseline; gap: 0.5rem; padding: 0.5rem 0; border-bottom: 1px dashed var(--border-color); }
.infoLabel { color: var(--text-secondary); font-weight: 500; }
.infoValue { color: var(--text-primary); }
.placeholder { text-align: center; padding: 3rem; }
.questionSetList { display: flex; flex-direction: column; gap: 1rem; }
.questionSetCard { display: flex; justify-content: space-between; align-items: center; padding: 1rem; background-color: var(--bg-main); border: 1px solid var(--border-color); border-radius: var(--border-radius-md); }
.questionSetTitle { font-weight: 500; color: var(--text-primary); }
.questionSetCount { color: var(--text-secondary); font-size: 0.9rem; }
.questionSetActions { display: flex; gap: 0.5rem; }
.actionBtn { background: var(--bg-content); border: 1px solid var(--border-color); color: var(--text-secondary); width: 36px; height: 36px; border-radius: 50%; display: flex; align-items: center; justify-content: center; cursor: pointer; transition: all var(--transition-speed); }
.actionBtn:hover { color: var(--text-primary); border-color: var(--accent-primary-solid); }
.deleteBtn:hover { color: #dc3545; border-color: #dc3545; }

/* --- Responsive Styles --- */
@media (max-width: 768px) {
    .profileContainer {
        grid-template-columns: 1fr; /* Switch to a single column layout */
    }
    .profileHeader {
        flex-direction: column;
        gap: 1rem;
        align-items: flex-start;
    }
    /* This new rule stacks the avatar and name vertically on mobile */
    .headerInfo {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.5rem;
    }
    .infoGrid {
        grid-template-columns: 1fr; /* Stack info items on top of each other */
    }

    /* Updated styles for the horizontal, scrollable sidebar */
    .profileSidebar nav {
        direction: rtl; /* This fixes the tab order and scroll direction */
        overflow-x: auto;
        padding-bottom: 10px; /* Add space for scrollbar if visible */
        margin-bottom: -10px; /* Counteract the padding */
        -ms-overflow-style: none;  /* IE and Edge */
        scrollbar-width: none;  /* Firefox */
    }
    .profileSidebar nav::-webkit-scrollbar {
        display: none; /* Hide scrollbar for Chrome, Safari and Opera */
    }
    .profileSidebar ul {
        display: flex;
        width: max-content; /* Ensure the container is wide enough for all items */
    }
    .sidebarLink {
        /* flex-shrink: 0;  */
        width: auto;
    }
}
